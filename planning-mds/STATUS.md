# Status

Last updated: 2026-05-08 (Milestone 10, M10.1–M10.6, complete. Claims worked example shipped as the second Fabric walkthrough alongside the policy one; `scripts/generation/generate-contract-inventory.py` and `generate-changelog.py` produce two new tracked artifacts (`docs/contract-inventory.md`, `CHANGELOG.md`); `docs/review-checklist.md` codifies the canonical and Fabric drift validators into a structured PR-review page; `versioning-policy.md` extended with a manifest-version surface, a regeneration cadence, and consumer-side pinning rules; M10.6 promoted every canonical contract from `draft` to at least `proposed`, with the four-contract policy walkthrough cohort plus its 10 supporting codesets advancing to `approved` (14 total). Both validators stay green: 85/85 canonical, 85/85 manifest drift.)

## Current Goal

Milestone 10 (W032, M10.1–M10.6) is complete. The canonical surface is at 0.4.x with 85 contracts: 71 at `proposed`, 14 at `approved`. The active goal moves to the **deferred scope** tracked under `planning-mds/IMPLEMENTATION_PLAN.md` §"Deferred Scope": risk-transfer contract family (W021), semantic projection (W009), additional targets beyond Fabric, and any future status-promotion waves (claims walkthrough cohort to `approved`, C4.5 commercial-lines spine to `approved`, etc.). None of the deferred-scope items is on the active path; each is acknowledged so the deferral is deliberate, not silent.

Prior milestones: Canonical hardening (Milestone 8.5, W025–W031, C1–C7) brought 85 contracts to 0.4.x against the strengthened validator. Microsoft Fabric Lakehouse target (Milestone 9, W023, F1–F8) shipped manifests + drift validator + Purview JSON + DDL + parameterized notebook templates + worked Policy walkthrough + orchestrator. Detailed plans: `planning-mds/CANONICAL_HARDENING_PLAN.md`, `planning-mds/FABRIC_IMPLEMENTATION_PLAN.md`, `planning-mds/MILESTONE_10_PLAN.md`.

## Guardrails

- Keep tracked files source-neutral.
- Do not commit private source review notes, raw source artifact names, URLs, copied definitions, raw schemas, ontology exports, or scratch mappings.
- Use private research only as design signal.
- Keep canonical contracts platform-neutral.
- Put implementation details under `targets/`.
- Put modeling rationale under `references/design-decisions/`.

## Current Repo State

- Repository structure is established.
- README, authoring guide, architecture notes, patterns, design decisions, and backlog exist.
- Cross-cutting design decisions are complete and indexed in `references/design-decisions/README.md` (identifier strategy, bi-temporal modeling, record state, codeset strategy, separation-and-nesting, currency, null semantics, data classification, versioning, status promotion, claims modeling, product-coverage M:N, risk-transfer scope, event-and-transaction). Three additional ADRs (`canonical-alignment.md`, `authoring-source-primacy.md`, `scd2-primary-key.md`) plus targeted ADR amendments land during canonical hardening.
- 85 P&C ODCS contracts at version 0.1.x–0.4.x pass the strengthened validator (`*_uid` GUID PKs, composite SCD2 PK on `(uid, valid_from_datetime)`, `record_status_code`, source-attribution, currency pairing, classifications, event/transaction correction-row enforcement, codeset relationship binding on every `*_code` field, `_uid`-suffix discipline). Status distribution post-M10.6: 71 `proposed`, 14 `approved` (the four-contract policy walkthrough cohort — `pc.policy`, `pc.policy-term`, `pc.policy-coverage`, `pc.policy-status-code` — plus the 10 codesets the cohort transitively references: `pc.line-of-business`, `pc.policy-type-code`, `pc.jurisdiction-code`, `pc.term-status-code`, `pc.currency-code`, `pc.coverage-status-code`, `pc.coverage-basis-code`, `pc.coverage-level-code`, `pc.source-system-code`, `pc.record-status-code`). Note: contracts were at `draft` from authoring through canonical hardening; M10.6 was the first phase to advance the `status:` field at scale. Canonical hardening C1–C7 had shipped end-to-end before M10: validator-first enforcement (C1, 12 rules), ADR / validator / glossary reconciliation (C2), bulk 0.3.0 refactor (C3), entity-gap closure with the C4.5 commercial-lines reversal (C4, 9 new contracts), codeset / reference-data hygiene (C5, 23 new codeset contracts; full relationship sweep), cross-source coherence and ADR back-links (C6), and single-contract cleanups (C7).
- Glossary covers cross-cutting and area-specific terms; `references/glossary/pc/cross-cutting.md` is the single source for shared concepts.
- Authoring guide and patterns cross-link the ADRs.
- Target folders exist but the dbt path has been retired in favor of a Fabric-native approach (W022 dropped). The Fabric target is planned per `planning-mds/FABRIC_IMPLEMENTATION_PLAN.md` (W023) and is gated on canonical hardening completion.

## Active Work Items

| ID | Work Item | Status | Notes |
| --- | --- | --- | --- |
| W001 | Fix policy placeholder path mismatch | Done | `Policy` now lives under `references/odcs/pc/policy/`. |
| W002 | Add planning tracker | Done | This file tracks execution status. |
| W003 | Add implementation plan | Done | See `planning-mds/IMPLEMENTATION_PLAN.md`. |
| W004 | Create ODCS authoring template | Done | Template lives at `references/odcs/templates/pc-contract-template.odcs.yaml`. |
| W005 | Add contract validation tooling | Done | `scripts/validation/validate-contracts.py` validates YAML shape, required metadata, naming, primary keys, descriptions, custom properties, and source-neutrality guardrails. |
| W006 | Author first milestone contracts | Done | `Party`, `PartyRole`, `PartyRelationship`, `Policy`, `Coverage`, `PolicyCoverage`, `Exposure`, exposure subtypes, `Claim`, and `FinancialTransaction` are complete and pass validation. |
| W007 | Add glossary starter set | Done | Property and Casualty starter glossary lives under `references/glossary/pc/`. |
| W008 | Add target type mapping guidance | Superseded | Replaced by W023.F1 (Fabric type mapping). dbt path retired with W022. |
| W009 | Define semantic and ontology projection approach | Not started | Treat ontology as derived semantic view, not as the canonical source of truth. |
| W010 | Add missing `Coverage` placeholder | Done | Roadmap and first milestone now align to `references/odcs/pc/coverage/coverage.odcs.yaml`. |
| W011 | Add missing referenced and dependent contracts | Done | Reference data plus `PolicyTerm`, `Product`, `PolicyLimit`, `PolicyDeductible`, `InsurableObject`, and `InsurableObjectClassification` are complete and pass validation. |
| W012 | Add submission contract set | Done | `Submission`, `SubmissionPartyRole`, `SubmissionRisk`, `SubmissionAssessment`, `SubmissionDocument`, and `SubmissionLifecycleEvent` are complete and pass validation. |
| W013 | Add policy lifecycle detail contracts | Done | `PolicyPartyRole`, `PolicyLifecycleEvent`, `PolicyTransaction`, and `PolicyDocument` are complete and pass validation. |
| W014 | Author cross-cutting design decisions | Done | Fourteen ADRs added/refined under `references/design-decisions/pc/` and indexed in the design-decisions README. |
| W015 | Bulk YAML refactor to align with new ADRs | Done | All 34 prior contracts refactored to v0.2.0 with `*_uid` PKs, SCD2 fields, `record_status_code`, source-attribution, and per-property classifications. Generator at `scripts/refactor/apply-adrs.py`. |
| W016 | Add `ProductCoverage` M:N junction contract | Done | `references/odcs/pc/coverage/product-coverage.odcs.yaml` per `product-coverage-modeling.md`. |
| W017 | Add missing codeset contracts | Done | 13 codeset contracts under `references/odcs/pc/reference-data/` generated via `scripts/refactor/generate-codesets.py`. |
| W018 | Fill in claims contract set | Done | `ClaimPartyRole`, `ClaimLifecycleEvent`, `ClaimFeature`, `ClaimCoverage`, `ClaimDocument`, `ClaimFinancialTransaction` are complete and pass validation. |
| W019 | Update validator to enforce new conventions | Done | Validator enforces `*_uid` PK suffix, classifications presence with allowed sensitivity / regulatory tags, SCD2 fields on entities, correction fields on event/transaction contracts, and `proposed` status. |
| W020 | Update authoring guide and patterns to reference new ADRs | Done | `docs/authoring-guide.md`, `references/patterns/pc/*`, and root `README.md` cross-link the ADRs. New `claim-lifecycle-pattern.md` added. |
| W021 | Risk-transfer contract family (deferred scope) | Deferred | Reinsurance, coinsurance, self-insurance, and fronting are deferred per `risk-transfer-scope.md`. Tracked here so the deferral does not get lost. |
| W022 | Author target guidance for dbt | Dropped | Retired in favor of Fabric-native projection. The user is Fabric-only and does not run dbt; supporting both targets is unnecessary scope. |
| W023 | Author target guidance for Microsoft Fabric | In progress | First and only target. Metadata-driven projection per `planning-mds/FABRIC_IMPLEMENTATION_PLAN.md`. Broken into F1–F8 phases below. Canonical hardening (W025–W031) is complete. F1 (docs only) is shipped; F2 (manifest generator + Policy golden example) is the next active phase, with a documented pause for review after F2. |
| W023.F1 | Fabric: conventions and type mapping | Done | All four F1 documents shipped under `targets/fabric/`: `README.md` (entry point — purpose, scope, persona flow, coexistence with `microsoft/skills-for-fabric`, file map), `type-mapping.md` (ODCS logical type → Spark SQL/Delta type, decimal precision rules, datetime/UTC semantics, GUID handling, codeset references, currency-paired amounts, type validation), `conventions.md` (single Silver Lakehouse `nebula_pc_silver`, schema-per-subject-area `silver_<area>`, materialization by contract kind — entity SCD2 / event-transaction append-only / codeset SCD2, partitioning, V-Order, Purview projection with sensitivity/regulatory tag mapping, HIPAA-aware handling, lakehouse binding), and `manifest-schema.md` (full schema reference for the `*.fabric.yaml` manifest with the `pc.policy` contract worked end-to-end as the golden example, plus event/transaction/codeset variations and 17 manifest-validator drift checks). Documentation only; no generators yet. |
| W023.F2 | Fabric: manifest generator and one example | Pause for review | `scripts/generation/generate-fabric-manifests.py` (reads ODCS YAML, projects field roles, codeset / foreign-key bindings, SCD2 / append-only / record-state blocks, projected + derived quality rules, bronze defaults, relationship pass-through; idempotent across reruns via digest-preserved `generatedAt`; CLI `--contract` to scope, `--generated-at` to override timestamp). `scripts/validation/validate-fabric-manifests.py` implements all 17 drift checks from `manifest-schema.md` §9 (path / id / version / digest / kind / mode-exclusion / SCD2 + append-only role coverage / forbidden-column / spark type set / type derivation / nullability / role coverage / FK + codeReference resolution / currency-pair consistency); `--require-full-coverage` flag for F3+. `targets/fabric/manifests/pc/policy/policy.fabric.yaml` is the golden manifest (pc.policy v0.4.2; entity kind; 22 columns with full role taxonomy; 9 source quality rules + 6 derived `accepted_values` rules; 10 relationships including 4 entity FKs and 6 codeset bindings). Validator passes cleanly on the emitted manifest. Pause for review per FABRIC_IMPLEMENTATION_PLAN.md §17 checkpoint before F3 fans out to all 85 contracts. |
| W023.F3 | Fabric: manifest generation for all 85 contracts | Done | Generator (`scripts/generation/generate-fabric-manifests.py`, no `--contract` filter) emitted 85 manifests under `targets/fabric/manifests/pc/` (1 carried over from F2 + 84 new). Manifest validator with `--require-full-coverage` passes cleanly: 0 drift findings across all 85 manifests. ContractKind distribution: 35 entity, 43 codeset, 3 event, 4 transaction (matches the post-C4 surface — `policy-lifecycle-event`, `submission-lifecycle-event`, `claim-lifecycle-event` for events; `policy-transaction`, `financial-transaction`, `policy-financial-transaction`, `claim-financial-transaction` for transactions). Mode mutual-exclusion holds across all kinds (entities + codesets enable `scd2` + `recordState`, never `appendOnly`; events + transactions enable `appendOnly`, never `scd2`/`recordState`). Role tally across all manifests: 85 `identity` (one PK per contract); 78 each of `scd2-valid-from` / `scd2-valid-to` / `scd2-is-current` / `record-state` (35 entity + 43 codeset = 78); 7 each of `event-correction-flag` / `event-corrects-ref` (3 events + 4 transactions); 2 `lifecycle-event-link` (transactions with `lifecycle_event_uid`); 105 `foreign-key`; 72 `code-reference`; 19 `monetary-amount` paired with 15 `monetary-currency` (4 amounts use the bound-currency exemption pattern from C3.4); 76 `source-attribution`; 58 `source-time` (the C7.4 `source_*_datetime` rename); 16 `business-key`; 461 `data`. Schema distribution: 9 silver_claims, 6 silver_core, 5 silver_coverage, 7 silver_exposure, 2 silver_financial, 6 silver_policy, 1 silver_product, 6 silver_submission, 43 silver_reference_data. F3-closeout fix: added `silver_product` to the schema enumeration in `targets/fabric/conventions.md` §1 and `planning-mds/FABRIC_IMPLEMENTATION_PLAN.md` §4 (preserves the W029 C5.6 decision that Product is its own subject area; `pc.product.subjectArea` stays as `product`). Manifests unchanged; no regen required. |
| W023.F4 | Fabric: Purview manifests | Done | `scripts/generation/generate-fabric-purview.py` reads all 85 Fabric manifests + the canonical glossary at `references/glossary/pc/` and emits two consolidated artifacts under `targets/fabric/purview/`. `sensitivity-labels.json` (schemaVersion 1.0) carries one column-level entry per Delta column (1235 total) with `fullyQualifiedName` (`Lakehouse://nebula_pc_silver/Tables/<schema>/<table>/<column>`), Purview `sensitivityLabel` mapped from canonical `PUBLIC/INTERNAL/CONFIDENTIAL/RESTRICTED`, `regulatoryTags` passed through (40 PII, 19 FINANCIAL on the current 0.4.x surface; PHI wired but no contract sets it yet), and source-contract traceability (`sourceContract` + `sourceContractVersion`); plus 85 table-level entries carrying the contract's `classificationProfile` (Internal 802 / Public 374 / Confidential 58 / Restricted 1 column distribution; Restricted slot is `pc.party.birth_date` per §11.1 of `targets/fabric/conventions.md`) and a `complianceProfile: HIPAA` flag whenever the source contract sets `customProperties.subjectToHipaa: true` (currently inactive on the 0.4.x surface — wired and ready). `business-glossary.json` (schemaVersion 1.0) carries 308 unique canonical terms harvested from `references/glossary/pc/cross-cutting.md` (h3 terms under h2 sections — 47 cross-cutting terms) plus the eight area files (h2 terms — claims 49, policy 37, reference-data 36, coverage 33, exposure 32, submission 28, financial 27, core 19); each term carries `name`, `definition` (first-paragraph description), `category` (filename stem), `sourcePath` with GitHub-flavored anchor (e.g. `references/glossary/pc/policy.md#policy-number`), `columnFQNs` (matched by case-insensitive normalized `businessName` against contract column metadata — e.g. "Source System Code" binds to all 38 `source_system_code` columns; "Claim Identifier" binds to 8 `claim_uid` FQNs across the claims schemas), and `tableFQNs` (matched by contract `name` — e.g. "Claim" binds to `silver_claims/claim`); 93 of 308 terms bind to at least one column or table FQN, the remainder describe abstract concepts (e.g. "Bi-Temporal Model", "Lifecycle Event") with no direct businessName surface. Generator is idempotent (rerun produces no diff); cross-check confirmed all 1235 sensitivity entries match the per-column `purview.sensitivityLabel` already emitted into the F2/F3 manifests. `validate-fabric-manifests.py --require-full-coverage` still passes 85/85. |
| W023.F5 | Fabric: DDL generation | Done | `scripts/generation/generate-fabric-ddl.py` reads all 85 Fabric manifests under `targets/fabric/manifests/pc/` and emits one `CREATE TABLE IF NOT EXISTS` per contract under `targets/fabric/ddl/pc/<area>/<slug>.spark.sql`, mirroring the manifest folder layout (9 claims, 6 core, 6 coverage, 7 exposure, 2 financial, 6 policy, 43 reference-data, 6 submission = 85). Each file: header comment naming manifest path, source contract id+version, contract kind, and the regeneration command; explicit column list with Spark SQL types from the manifest (STRING / INT / BIGINT / BOOLEAN / DATE / TIMESTAMP / DECIMAL(18, 2)); `NOT NULL` on non-nullable columns; per-column `COMMENT` derived from the contract field description (single-quote-doubled for SQL); fully-qualified table name `nebula_pc_silver.<schema>.<table>` (Product picks up `silver_product` per the C5.6 / F3 closeout decision even though its manifest sits under the coverage folder); `USING DELTA`; `PARTITIONED BY` from `fabric.table.delta.partitionedBy` (entities + codesets partition by `is_current_indicator`; events partition by `event_datetime`; transactions partition by `transaction_effective_date` or equivalent); table-level `COMMENT` carrying the contract description plus `Source: pc.<contract-id> v<version>.`; `TBLPROPERTIES` block with `delta.appendOnly` (true for events/transactions, false otherwise), `delta.autoOptimize.autoCompact`, `delta.autoOptimize.optimizeWrite`, `delta.enableChangeDataFeed` rendered alphabetically with both sides quoted; trailing `OPTIMIZE … ZORDER BY (…)` advisory comment when `zorderBy` is non-empty (entities + codesets get a `ZORDER BY (<pk_uid>)` hint; events and transactions skip it per `conventions.md` §7). Generator is idempotent (re-run produces no diff), validates Spark types against an allow-list, and prints a per-kind / per-area tally on each run. `validate-fabric-manifests.py --require-full-coverage` still passes 85/85 — DDL generation is read-only against the manifest set. |
| W023.F6 | Fabric: notebook templates | Done | `scripts/generation/generate-fabric-notebooks.py` emits three parameterized notebook templates plus the lakehouse-binding companion under `targets/fabric/notebooks/`: `silver-scd2-merge-template.ipynb` (entity + codeset SCD2 merge — close-old / insert-new / optional deletion-aware soft-delete; SHA-256 change detection over manifest's non-system columns; auto-creates the Delta table on first run from the manifest's column list and table properties), `silver-append-template.ipynb` (event + transaction insert-only with idempotent re-run guard on `*_uid` and pre-write correction-reference resolution), `silver-codeset-load-template.ipynb` (codeset SCD2 with deletion-aware default in full-refresh mode, automatically off in incremental mode). Each notebook follows the canonical 12-cell layout per `targets/fabric/conventions.md` §9.2: 1 markdown intro + 11 code cells (parameter cell tagged `parameters`, imports + Spark/V-Order/UTC config, manifest load + structural sanity check, type helpers + projection, kind guard, Bronze read with `incremental` / `full` / `catchup` modes, pre-write quality assertions over `not_null` / `unique` / `expression` / `currency_pair` / `accepted_values` rule types, materialization body, post-write codeset-reference + SCD2-window + current-row-uniqueness checks, advisory `OPTIMIZE ... ZORDER`, structured run summary as JSON). All four parameter knobs surfaced (`manifest_path`, `load_mode`, `as_of_datetime`, `bronze_prefix_override`) plus `run_optimize` / `optimize_min_rows` for the rewrite gate. Generator validates the emitted `.ipynb` shape against the Fabric / `skills-for-fabric` requirements: every code cell has `outputs: []` and `execution_count: null`; `metadata.dependencies.lakehouse` carries empty `default_lakehouse` / `default_lakehouse_name` / `default_lakehouse_workspace_id` slots; exactly one cell per notebook is tagged `parameters`. `lakehouse-binding-template.json` ships with the same three empty fields and a comment field naming the deployer-filled convention. Generator is idempotent (`--check` flag validates without writing); every code cell parses as syntactically-valid Python; manifests are not consumed at generation time, only at notebook runtime. `validate-fabric-manifests.py --require-full-coverage` still passes 85/85 (notebook generation is decoupled from manifests). |
| W023.F7 | Fabric: worked-example walkthrough | Done | `targets/fabric/examples/end-to-end-policy.md` traces `pc.policy` (entity, 0.4.2), `pc.policy-term` (entity, 0.4.1), `pc.policy-coverage` (entity, 0.4.1), and `pc.policy-status-code` (codeset, 0.4.0) through the full six-persona flow defined in FABRIC_IMPLEMENTATION_PLAN.md §3.3.3. Sections cover: persona-flow recap (architect → generator → platform engineer → operator → analyst → governance); the four-contract dependency graph (codeset → policy_term → policy → policy_coverage); a Step 1 architect-edit example (adding an `IN_FORCE_PENDING_AUDIT` codeset value with version bump and changelog entry) followed by the canonical-validator gate; the Step 2 generator run (manifest + DDL + Purview + notebook regen, ending with `validate-fabric-manifests.py --require-full-coverage`) with manifest excerpt for `pc.policy`, DDL excerpt for `silver_policy.policy`, Purview JSON excerpt showing all four table-level entries (Policy + PolicyTerm Confidential, PolicyCoverage Internal, PolicyStatusCode Public) and notable column entries (`policy_description` Confidential+PII; `annualized_premium_amount` Confidential+FINANCIAL); the Step 3 deployer-choice matrix (`spark-authoring-cli` / Fabric REST / Azure DevOps / manual upload) emphasizing that this repository ships file artifacts and the deployment mechanism is consumer-driven, with the explicit artifact bundle the deployer consumes for the four contracts; the Step 4 operator load order (codeset before entities; `policy_coverage` last) with structured-stub run-summary JSON for all four notebook runs (matching `SHARED_RUN_SUMMARY_SOURCE` from `generate-fabric-notebooks.py`) plus a worked failure-mode trace (running `policy` before the codeset triggers a `policy_status_code_in_codeset` post-write assertion failure that re-runs cleanly once the codeset is loaded); the Step 5 analyst SQL (a four-table SCD2 join with `is_current_indicator = TRUE` filters and a codeset-side `code_value` join, plus an SCD2 time-travel pattern); the Step 6 Purview ingest (1235 column entries + 85 table entries + 308 glossary terms; "Source System Code" linked to 38 columns lakehouse-wide); a reverse-blast-radius trace showing what regenerates when the architect *deletes* the new codeset value; and a 36-entry cross-reference table that resolves to actual files in the repo. F7 acceptance criteria met: each persona owns one step; each cross-reference resolves to a tracked file (verified); §3.3 boundary is concrete (the example names the exact files step 3 consumes and the live-workspace state step 3 produces). The walkthrough deliberately uses a structured stub for the merge execution trace because real Fabric Spark execution is a downstream verification, not a generation-time step. Manifest validator still passes 85/85; the walkthrough is documentation only. |
| W023.F8 | Fabric: status, planning, validator closeout | Done | `scripts/generation/generate-fabric.py` orchestrator runs the four sub-generators in dependency order — manifests, Purview, DDL, notebooks — and ends with `validate-fabric-manifests.py --require-full-coverage` so a green orchestrator run is the same green check FABRIC_IMPLEMENTATION_PLAN.md §3.3.3 step 2 describes. CLI surfaces a `--skip-validate` flag for development workflows where partial output is intentional. Each step runs as a child Python process via subprocess so each sub-generator owns its own argparse / output / exit code; the orchestrator stops at the first non-zero exit so a downstream artifact never reflects an upstream half-finished state, and emits a per-step summary table (label / elapsed / OK or FAIL or SKIPPED). End-to-end run on the 0.4.x surface is clean: 85 manifests emitted, 1235 column-level Purview labels + 308 glossary terms emitted, 85 DDL files emitted, 3 notebook templates + lakehouse-binding emitted, 85 manifests validated 0 findings; reruns are idempotent (no diff). Root `README.md` Target Implementations section now lists the full Fabric artifact tree, generators, validator, and recommended starting points (`targets/fabric/README.md`, `examples/end-to-end-policy.md`, `FABRIC_IMPLEMENTATION_PLAN.md`). `docs/authoring-guide.md` validation flow section gains a Fabric-target paragraph naming `generate-fabric.py` as the canonical drift check and explaining manifest-edit prohibition. `planning-mds/IMPLEMENTATION_PLAN.md` updated: Current Baseline, Milestone 9 status, and First Execution Order all reflect F1–F8 closure; remaining work narrowed to Milestone 10 + deferred scope. |
| W024 | Glossary refresh for new conventions | Done | Added `references/glossary/pc/cross-cutting.md` (single source for shared terms); refreshed area files for GUID vs business-key identifiers, ProductCoverage, the six new claims contracts, immutability/correction language, and the new codesets. Glossary index updated to point at `cross-cutting.md` first. |
| W025 | Canonical hardening C1 — validator-first enforcement | Pause for review | 12 new validator rules implemented in `scripts/validation/validate-contracts.py` (C1.1–C1.12). Unit tests in `scripts/validation/tests/test_hardening_rules.py` (35 tests, all green). Run produces 195 errors + 7 warnings (202 total) across the unmodified 0.2.0 surface; deterministic punch list at `planning-mds/CANONICAL_HARDENING_PUNCH_LIST.md`. By rule: C1.1=10, C1.2=160, C1.5=8, C1.6=3 (warning), C1.7=17, C1.8=4 (warning); C1.3, C1.4, C1.9, C1.10, C1.11, C1.12 produce zero findings on 0.2.0 (no unresolved targets, all append-only contracts have `corrects_*_uid`, every `classificationProfile` ≥ max field sensitivity, no contracts at `approved`, no version bumps vs HEAD, no `customProperties.adrs` yet). No contract edits made. Pause for review per CANONICAL_HARDENING_PLAN.md §6 checkpoint 1 before C2 begins. |
| W026 | Canonical hardening C2 — ADR / validator / glossary reconciliation | Pause for review | All five C2 reconciliations landed plus two reviewer-driven amendments (source_system_code policy + C1.8 widening). `identifier-strategy.md` codifies the shipped `*_uid / *_number / *_code` triad, treats `*_id` as a dead-letter, and binds `source_system_code` to a `source-system-code` codeset (no exemption). `review` dropped from `ALLOWED_STATUSES`. `status-promotion.md` rephrased to split validator-enforced from process-enforced gates and match what C1.10 actually checks. `null-semantics.md` addendum distinguishes business-meaning booleans from null-presence indicators. `data-classification.md` clarifies narrative-free-text default and codeset carve-out. C1.8 widened to flag opaque `*_uid` GUIDs tagged `RESTRICTED + PII` (catches `lifecycle-event-type.resulting_lifecycle_status_uid`). `source-system-code` added to the C5.1 codeset slate in `CANONICAL_HARDENING_PLAN.md`. Validator + 39 unit tests pass; punch list now 203 findings (was 202; +1 from the C1.8 widening). Pause for review per CANONICAL_HARDENING_PLAN.md §6 checkpoint 2 before C3 begins. |
| W027 | Canonical hardening C3 — bulk 0.3.0 refactor | Done | All 54 contracts at 0.3.0 with named changelog entries. `scripts/refactor/apply-hardening-c3.py` is idempotent. Landed: composite SCD2 PK on `(uid, valid_from_datetime)` per new `scd2-primary-key.md` ADR (C3.1); dropped `created/updated_datetime` from `policy-lifecycle-event`, `policy-transaction`, `submission-lifecycle-event`, `financial-transaction` (C3.2); dropped mutable `transaction_status_code` from `policy-transaction` and `financial-transaction` (C3.3); currency pairing on 4 exposure subtypes plus 2 renames in `policy-term`/`policy-transaction` plus 4 written exemptions for shared-currency bound amounts in `policy-deductible`/`product-coverage` (C3.4 + 2 user-folded extras + bound-currency exemption pattern); over-classification re-tagged to INTERNAL on 5 fields including `lifecycle-event-type.resulting_lifecycle_status_uid` (C3.5); 17 narrative free-text fields lifted to CONFIDENTIAL + PII (C3.6); 3 redundant `*_uid + *_code` pairs dropped, relationships rewritten to point at `code_value` on the codeset (C3.7); YAML anchors expanded throughout (C3.8); single bulk version bump (C3.9). `temporal-modeling.md` corrected mid-phase (append-only contracts no longer claim to carry `valid_from_datetime`). Validator now flags 155 findings (was 203); the remainder is C1.2 codeset-relationship binding, addressed in C5. Tests still 39/39 green. |
| W028 | Canonical hardening C4 — canonical entity gaps | Done | Nine new contracts shipped at 0.1.0: `pc.occurrence` (C4.1), `pc.catastrophe` (C4.2), `pc.insurable-object-party-role` (C4.3), `pc.policy-financial-transaction` + `pc.financial-transaction-classification` (C4.6: ship), and the C4.5 commercial-lines reversal — `pc.account`, `pc.account-relationship`, `pc.account-party-role`, `pc.agreement` (user-driven scope expansion: commercial-lines focus). `pc.claim` bumped to 0.4.0 with `insurable_object_uid` (C4.4), `occurrence_uid` (C4.1), `catastrophe_uid` (replacing `catastrophe_code`, C4.2), and `account_uid` (C4.5); added `claim_insurable_object_path_consistent` quality rule. `pc.policy` and `pc.submission` bumped to 0.4.0 with `account_uid` + `agreement_uid` FKs (C4.5). `pc.policy-financial-transaction` bumped to 0.2.0 with `account_uid` + `agreement_uid` FKs (C4.5). Catastrophe `start_datetime`/`end_datetime` renamed to `effective_datetime`/`expiration_datetime` to follow the now-documented universal business-window convention in `temporal-modeling.md`. Generic `pc.party-role` deleted (C4.7); `claim-party-role.party_role_type_code` renamed to `role_type_code` (C4.8); `policy-document` and `submission-document` aligned on the claim-document shape (C4.9, both 0.4.0). New pattern doc: `references/patterns/pc/account-pattern.md`; `party-role-pattern.md` updated to add `AccountPartyRole`. Validator's `EVENT_OR_TRANSACTION_SLUGS` updated for `policy-financial-transaction`. C5.1 codeset slate extended by 9 new entries (4 occurrence/catastrophe + 5 account/agreement). Punch list: 184 findings (was 166): C1.2=172, C1.3=12. The remaining findings are all codeset-binding (C5 territory). 39 unit tests still green. **Note:** §6 checkpoint 3 effectively reset by the C4.5 commercial-lines reversal; C4 now closes as `done` with the expanded scope rather than pausing for review. |
| W029 | Canonical hardening C5 — codeset and reference-data hygiene | Done | 23 new codesets shipped (C5.1, two original-slate entries — `transaction-classification-code` and `expense-classification-code` — folded into the C4-shipped `pc.financial-transaction-classification`). `scripts/refactor/apply-hardening-c5.py` is idempotent. Codeset relationship sweep added bindings on every `*_code` field with a matching codeset and `customProperties.codesetExempt: true` plus rationale on the long tail of carrier-product / industry-library codes (C5.2). Codeset-strategy ADR gained the pure-codeset vs reference-data-entity addendum (C5.3) plus the `record-status-code` self-reference note (C5.8). 14 pure codesets bumped to 0.4.0 with `classificationProfile: PUBLIC` and all-PUBLIC field sensitivities; 4 richer reference-data entities (lifecycle-event-type, lifecycle-status, line-of-business, transaction-type) lifted `code_value` to PUBLIC (C5.4 / C5.7). `*_status_code` rename sweep brought 6 contracts into `<schema_name>_status_code` form (insurable-object-classification, lifecycle-event-type, lifecycle-status, line-of-business, transaction-type, policy-term — plus `party.status_code` → `party_status_code` to close the long tail; C5.5). `pc.product` `subjectArea` set to `product` (C5.6). Validator + 39 unit tests both green; punch list deleted. |
| W030 | Canonical hardening C6 — cross-source coherence and authoring discipline | Done | New ADRs landed: `canonical-alignment.md` (deliberate departures + deferrals across the canonical surface; identifier compactness, composite SCD2 PK, no generic PartyRole, polyclass exposure subtypes, `Account`/`Agreement` C4.5 reversal, codesetExempt long tail; deferrals: risk-transfer, litigation, full assessment hierarchy, semantic projection, additional targets, bronze) and `authoring-source-primacy.md` (ADR > pattern > glossary > contract > validator with mechanical disagreement resolution). Both ADRs registered in `references/design-decisions/README.md` under "Governance" (C6.1, C6.2). All 85 contracts now carry `customProperties.adrs: [...]` listing the ADRs that govern each contract's shape, computed by `scripts/refactor/apply-hardening-c6.py` based on path + field detection (universal: identifier-strategy, data-classification, versioning-policy, status-promotion, canonical-alignment, authoring-source-primacy; SCD2 entities add temporal-modeling, record-state, scd2-primary-key; events/transactions add temporal-modeling, event-and-transaction; `*_code` fields add codeset-strategy; `*_amount` fields add currency-convention; subject-area-specific ADRs per file path) (C6.3). Per-contract changelog discipline (name specific ADRs going forward) documented in `docs/authoring-guide.md`; existing pre-0.3.0 entries left as git history (C6.4). `docs/authoring-guide.md` updated with a new "Authoring Source Primacy" section, the C6.3 ADR back-link convention, and an updated workflow that names which ADR drove which field addition; entity-contract skeleton extended with `adrs: [...]` example (C6.5). `docs/repository-and-architecture.md` refreshed: repository tree reflects post-C4 shape (account-relationship, account-party-role, insurable-object-party-role, occurrence, catastrophe, financial-transaction-classification, claim-feature, claim-lifecycle-event, claim-financial-transaction, product-coverage, account-pattern), ADR/pattern indices include the two new ADRs and `account-pattern.md`, and a new "Commercial Lines Boundary" section documents the C4.5 Account/AccountRelationship/AccountPartyRole/Agreement spine plus the optional `account_uid`/`agreement_uid` FKs on Policy/Submission/PolicyFinancialTransaction (C6.6). `party-role-pattern.md` and `financial-transaction-pattern.md` cross-reference `canonical-alignment.md` for the deliberate-departure rationale (no generic `PartyRole`; separate `PolicyFinancialTransaction`/`ClaimFinancialTransaction` rather than a single polymorphic FinancialTransaction); patterns README updated to add `account-pattern.md` (C6.7). Validator passes with zero findings on all 85 contracts; C1.12 ADR-id resolution rule confirms every id resolves; 39 unit tests still green; refactor script idempotent. Per CANONICAL_HARDENING_PLAN.md §3, C6 does not bump contract versions — `customProperties.adrs` additions are governance/documentation metadata. |
| W031 | Canonical hardening C7 — single-contract cleanups | Done | All five C7 deliverables shipped via `scripts/refactor/apply-hardening-c7.py` (idempotent). C7.1: `vehicle-exposure.vehicle_identifier` renamed to `vin_number` with businessName "VIN Number" and a `*_number` business-key description. C7.2: `submission-lifecycle-event.triggering_transaction_uid` dropped (no `submission-transaction` contract exists; submission lifecycle is event-only). C7.3: `submission` gains a `submission_outcome_dates_mutually_exclusive` quality rule (at most one of `bind_date` / `decline_date` / `withdrawn_date` populated per snapshot). C7.4: standardized rename across 28 entity / reference-data contracts — `created_datetime` → `source_created_datetime`, `updated_datetime` → `source_updated_datetime` — with rewritten descriptions naming source-system-time semantics distinct from SCD2 system time. C7.5: `identifier-strategy.md` amended to scope `source_natural_key` to the primary source key only; multi-source provenance is an MDM concern outside the canonical layer until a use case requires a `*-source-provenance` 1:N child contract. Validator's C1.5 rule extended to ban the new `source_*_datetime` names on append-only contracts (forward defense); `temporal-modeling.md` gains a paragraph documenting source-time as a third axis on entity contracts. Per-contract patch bumps applied to 30 contracts (0.3.0 → 0.3.1, 0.4.0 → 0.4.1, 0.4.1 → 0.4.2) with combined C7 changelog entries naming each issue closed. Validator passes with zero findings on all 85 contracts; 40 unit tests green (was 39; added a C1.5 source-name fixture). |
| W032 | Milestone 10 — examples, docs, release governance | Done | All six M10 phases shipped per `planning-mds/MILESTONE_10_PLAN.md`. **M10.1**: `targets/fabric/examples/end-to-end-claims.md` ships the second worked walkthrough — six contracts (Claim, ClaimFeature, ClaimLifecycleEvent, ClaimFinancialTransaction, ClaimStatusCode, FinancialTransactionClassification) exercising append-only event, append-only transaction with `lifecycle-event-link`, and the C4.5 commercial-lines spine that the policy walkthrough deliberately leaves out. **M10.2**: `scripts/generation/generate-contract-inventory.py` + `docs/contract-inventory.md` — single tracked navigation page covering all 85 contracts by Spark schema, kind, version, status, description, and ADR back-links; idempotent with a `--check` flag. **M10.3**: `scripts/generation/generate-changelog.py` + `CHANGELOG.md` — repo-level changelog aggregating per-contract entries grouped by canonical-version wave (0.4.x → 0.1.x). 318 entries post-M10.6 (was 233 pre-M10.6). **M10.4**: `docs/review-checklist.md` — structured PR review page organized by contract kind (entity / event / transaction / codeset), cross-cutting checks, status-promotion review, Fabric impact matrix, source-neutrality boundary; cross-linked from `docs/authoring-guide.md`. **M10.5**: `references/design-decisions/pc/versioning-policy.md` extended with three new sections — manifest version (`*.fabric.yaml` `manifestVersion`), Fabric artifact regeneration cadence, and consumer-side pinning patterns (contract-version pin / manifest-version pin / repo-tag pin). **M10.6**: `scripts/refactor/apply-milestone-10-status.py` (idempotent) advanced every canonical contract from `draft` to at least `proposed` and the 14-contract approved cohort to `approved` — 4 policy-walkthrough contracts plus 10 transitively-referenced codesets (`pc.line-of-business`, `pc.policy-type-code`, `pc.jurisdiction-code`, `pc.term-status-code`, `pc.currency-code`, `pc.coverage-status-code`, `pc.coverage-basis-code`, `pc.coverage-level-code`, `pc.source-system-code`, `pc.record-status-code`). Each promoted contract bumped PATCH per `versioning-policy.md`. Fabric orchestrator regenerated all 85 manifests + DDL + Purview JSON + notebooks; manifest validator still 85/85 OK; canonical validator still 85/85 OK. Plan note: the §3.3 cohort table in `MILESTONE_10_PLAN.md` originally listed 9 codesets; verification against the actual `relationships:` blocks surfaced `pc.jurisdiction-code` (referenced by `pc.policy.jurisdiction_code`) as a missing entry, and the cohort grew to 10. Held back at `proposed`: claims walkthrough cohort (M10.1 should settle for one milestone first), C4.5 commercial-lines spine, submission area, exposure subtypes, financial-transaction family, and the reference-data tail with no documented downstream consumer. |

## Next Recommended Step

Milestone 10 (W032, M10.1–M10.6) is complete. The canonical layer surface is at version 0.1.x–0.4.x with 71 `proposed` and 14 `approved` contracts; the Fabric target is fully shipped; documentation, inventory, changelog, and review checklist are all in place.

Active focus moves to the **deferred scope** tracked under `planning-mds/IMPLEMENTATION_PLAN.md` §"Deferred Scope". None of these are on the immediate active path; each is acknowledged so the deferral is deliberate:

1. **Risk-transfer contract family** (W021) — reinsurance, coinsurance, fronting, self-insurance per `risk-transfer-scope.md`.
2. **Semantic projection** (W009) — RDF / OWL / SKOS / knowledge-graph derivation of the canonical surface.
3. **Targets beyond Fabric** — Databricks, Snowflake, Kafka, API. Out of scope for the current milestone wave.
4. **Future status-promotion waves** — promoting the claims walkthrough cohort (`pc.claim`, `pc.claim-feature`, `pc.claim-lifecycle-event`, `pc.claim-financial-transaction`, `pc.claim-status-code`, `pc.financial-transaction-classification`) to `approved` once the M10.1 walkthrough has settled; promoting the C4.5 commercial-lines spine (`pc.account`, `pc.agreement`, `pc.account-relationship`, `pc.account-party-role`) once a real downstream consumer exists.
5. **Production release tagging.** The repo is ready to be tagged but the tag itself is a consumer-driven operation, not part of this milestone wave.

Sequencing: **W025 (C1, done) → W026 (C2, done) → W027 (C3, done) → W028 (C4, done) → W029 (C5, done) → W030 (C6, done) → W031 (C7, done) → W023.F1 (done) → F2 (done) → F3 (done) → F4 (done) → F5 (done) → F6 (done) → F7 (done) → F8 (done) → W032 (M10.1–M10.6, done) → deferred scope**.
